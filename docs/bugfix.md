1. 前端群聊会话窗口上方群聊标题旁边固定显示（0人），而不是根据群聊人数显示，请你修复
2. GetGroupMembers函数只是查表，没有判断status是否为6，如果status为6的话就表明退群了，请你修改，以及适配其他相关地方的逻辑
3.前端增添一个功能：打开一个会话的时候校验是否是好友/是否为群成员/群聊是否存在，如果不是的话，在会话窗口显示出来，前端增加对应的接口，后端也增加对应的接口实现。
4.在进行相关群聊操作的时候，完善校验逻辑，比如在进行退出群聊操作的时候，要校验群是否被解散。其他群聊操作同理，请你都检查一下
5.在进行收发消息的时候，A向B发出消息，B收到消息，但没有打开与A的会话窗口，前端只展示红色小点，没有自动弹出对应的会话窗口，用户需要手动去好友列表里点击对应的好友才能打开会话窗口，这是不符合用户体验的，请你修复

- 群聊可重复解散